language: node_js
node_js:
  - node
install:
  - npm install
  - npm install -g codecov
script:
  - npm test -- --coverage && codecov
# deploy:
#   provider: pages
#   skip-cleanup: true
#   github-token: $GITHUB_TOKEN
#   keep-history: true
#   on:
#     branch: master
